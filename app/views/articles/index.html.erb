<h1>Listing articles</h1>

<table>
	<tr>
		<th>Title</th>
		<th>Text</th>
	</tr>

	<% @articles.each do |a| %>
	 <tr>
	 	<td><%= a.title %></td>
	 	<td><%= a.text %></td>
	 	<td><%= link_to 'Show', article_path(a) %></td>
	 </tr>
  <% end %>
</table>